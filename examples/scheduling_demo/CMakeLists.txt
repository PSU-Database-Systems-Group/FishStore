set(BENCHMARK_HEADERS
        "scan_contexts.h"
        "misc.h"
        "table_scans.h"
        "types.h"
        "legacy.h"
        "filter.h"
        "projection.h"
        "record_helper.h"
)

set(BENCHMARK_SOURCES
)

ADD_FISHSTORE_BENCHMARK(scheduling_demo)